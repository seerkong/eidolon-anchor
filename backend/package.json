{
  "name": "eidolon-anchor-backend",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "../shared"
  ],
  "scripts": {
    "dev": "cd packages/elysia && bun run dev",
    "start": "cd packages/elysia && bun run start",
    "build": "cd packages/elysia && bun run build",
    "dev:koa": "cd packages/koa && bun run dev",
    "start:koa": "cd packages/koa && bun run start",
    "build:koa": "cd packages/koa && bun run build",
    "test": "cd packages/core && bun run test && cd ../composer && bun run test"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "bun-types": "latest",
    "typescript": "^5.4.0"
  }
}
