<ToolPrompt #WorkflowPatchBundle.detail>
(
  <usage ?>Use only for compatibility planning. Real edits import the definition into a recoverable session and mutate /work through WorkflowWorkspace before proof and publication.</?>
  <safety ?>Reject unsafe refs through the component validation result. Never treat a host absolute path as a workflow manifest ref.</?>
)>
