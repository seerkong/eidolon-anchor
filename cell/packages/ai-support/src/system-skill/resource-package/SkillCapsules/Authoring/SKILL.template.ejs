---
name: <%= metadata.name %>
description: <%= metadata.description %>
---

# <%= metadata.title %>

Use exact structured authoring operations. The coding stage context already includes this Skill root and `operations/index.md`; do not load them again.

Call `WorkflowOpenAuthoringSession` immediately without querying App, reusable-Agent, template or prebuilt lists. Its deterministic default opens an existing workspace ResourcePackage. If the exact result says that no complete workspace package exists, load `operations/create-resource-package.md` with only the exact required grammar references and call `WorkflowCreateResourcePackageSession` with one complete file set. Do not fall back to `WorkflowCreateBundle`. Opening or creating the complete package does not choose semantic topology.

After the session exists, load `sys-halfcode-resource-dsl` for the generic XNL resource contract. For workflow Kind/profile semantics, first read `references/flow-dsl/README.md`, then load only the exact declared reference needed for the selected Kind or profile. Use `WorkflowWorkspace(operation=read_selection)` before requesting further source files.

The model chooses the resource Kind, profile, topology, and operation from installed standards. Deterministic components own parsing, containment, revision checks, CAS application, digests, build/proof evidence, and publication transitions. A successful edit does not authorize publication or execution, and runtime facts never become authoring source.
