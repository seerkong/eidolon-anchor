---
name: <%= metadata.name %>
description: <%= metadata.description %>
---

# <%= metadata.title %>

Start with:

```json
{ "skill": "sys-eidolon-anchor-run", "resource": "operations/index.md" }
```

Use only explicit published resource references, instance/binding identities, typed runtime transitions, and evidence returned by native tools. Resolve or inspect only the exact missing fact; do not load authoring grammar or alter a published definition.

Run facts do not rewrite authoring source. A `Cancelled` outcome is valid only where an existing typed wait-resume protocol explicitly accepts it; this Skill does not define a general cancellation operation.
