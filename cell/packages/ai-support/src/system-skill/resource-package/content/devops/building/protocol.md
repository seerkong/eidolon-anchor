# Building protocol

Use the exact session and working revision returned by coding. For ResourcePackage authoring, preserve the complete package and invoke the Halfcode/depa preparation surface once for that revision; do not validate a detached workflow file as if it were the package. A diagnostic returns to `coding` with its exact location and code; a successful build advances to `testing`. Do not use explanation text or repeated summaries as build evidence.
