# Monitoring system context

Read-only observation of run status, graph/events, result, Material receipts, replay, and evidence. Load `Skill({ skill: "sys-eidolon-anchor-run" })` and `Skill({ skill: "sys-eidolon-anchor-run", resource: "operations/index.md" })`.

Never alter the historical run or authoring definition while observing it.
