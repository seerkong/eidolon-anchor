# Operating protocol

Select the exact start or wait-resume operation resource from current instance/run facts. With an existing instance and execution authorization, invoke the corresponding native tool once and preserve its run receipt. With no instance identity, return to `deploying`; with a terminal result, deliver it or move to `monitoring` only for requested evidence detail.
