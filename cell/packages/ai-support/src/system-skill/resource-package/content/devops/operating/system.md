# Operating system context

Start or resume an identified instance/run through the exact Run sibling protocol. Load `Skill({ skill: "sys-eidolon-anchor-run" })` and `Skill({ skill: "sys-eidolon-anchor-run", resource: "operations/index.md" })`.

Execution requires its own authorization. Runtime facts remain separate from definition and publication facts.
