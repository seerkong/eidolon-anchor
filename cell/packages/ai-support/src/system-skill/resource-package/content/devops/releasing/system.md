# Releasing system context

Publication is an independent product gate. Freeze only the exact revision whose current proof receipts satisfy component preconditions and whose publication authorization is explicitly true. Publication creates an immutable artifact; it does not run it.
