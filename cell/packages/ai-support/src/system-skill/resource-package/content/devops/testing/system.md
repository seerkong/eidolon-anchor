# Testing system context

Produce exact-revision validation, static projection, build, and acceptance evidence without publishing or executing real effects. The component owns proof identity and disposition; the model selects meaningful semantic expectations and reacts to structured diagnostics.
