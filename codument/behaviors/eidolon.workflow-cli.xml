<behaviors capability="eidolon.workflow-cli" version="1">
  <requirement id="component-backed-authoring">
    <statement>The Eidolon CLI SHALL expose workflow authoring commands that reuse the Eidolon workflow component/service instead of implementing independent workflow core logic.</statement>
    <suite id="workflow-init" name="Workflow init">
      <case id="init-writes-controlled-draft">
        <given>a user runs `eidolon workflow init ai-data demo`</given>
        <when>the command creates files</when>
        <then>the CLI SHALL obtain the bundle draft from the workflow component and write the returned files under the selected `.eidolon` target root</then>
      </case>
    </suite>
  </requirement>
  <requirement id="workflow-roots-metadata">
    <statement>Headless Eidolon runtime metadata SHALL include AI workflow roots so native workflow tools can detect workspace/global workflow roots during CLI conversations.</statement>
    <suite id="exec-runtime-metadata" name="Exec runtime metadata">
      <case id="inject-aiworkflow-roots">
        <given>headless exec runtime metadata is built</given>
        <when>WorkflowInspectCapability runs in that runtime</when>
        <then>the tool SHALL report workflowRootsInjected=true</then>
      </case>
    </suite>
  </requirement>
  <requirement id="heredoc-session-continuation">
    <statement>The Eidolon workflow CLI MUST accept explicit heredoc-backed user instructions and MUST continue the same Eidolon conversation across independent CLI processes when a session key is supplied.</statement>
    <suite id="workflow-agent-input" name="Workflow agent input">
      <case id="explicit-stdin-source">
        <given>A user invokes `eidolon-cli workflow agent /dev/stdin` or `eidolon-cli workflow agent -` with a shell heredoc</given>
        <when>The workflow agent resolves the requirement</when>
        <then>The complete multi-line stdin body MUST become the ordinary-language request without treating the source token as text</then>
      </case>
      <case id="positional-compatible">
        <given>A user supplies an ordinary single-line requirement positionally</given>
        <when>The workflow agent resolves the requirement</when>
        <then>The original positional behavior MUST remain unchanged and MUST NOT wait for stdin</then>
      </case>
    </suite>
    <suite id="installed-continuation" name="Installed CLI continuation">
      <case id="three-process-journey">
        <given>A locally installed Eidolon CLI and one stable session key</given>
        <when>three independent heredoc calls create, authorize publication, then authorize execution of a public multi-source workflow</when>
        <then>The CLI MUST recover one Eidolon conversation and return a report grounded in the requested public sources without merging publication and execution authorization</then>
      </case>
    </suite>
  </requirement>
  <requirement id="unified-eidolon-executable">
    <statement>The project SHALL ship one eidolon executable that owns the interactive TUI and all headless, workflow and global CLI subcommands.</statement>
    <suite id="unified-entry" name="Unified executable entry">
      <case id="default-tui">
        <given>A user invokes `eidolon` with no subcommand or with a project path</given>
        <when>The unified command parses the invocation</when>
        <then>It SHALL launch the interactive TUI</then>
      </case>
      <case id="shared-subcommands">
        <given>A user invokes `eidolon workflow`, `eidolon global`, `eidolon exec` or another registered headless subcommand</given>
        <when>The unified command parses the invocation</when>
        <then>It SHALL execute that subcommand from the same compiled program</then>
      </case>
      <case id="no-parallel-cli-distribution">
        <given>The project build and install surfaces are inspected</given>
        <when>Terminal artifacts are produced</when>
        <then>There MUST be no eidolon-cli command, headless-only entry, package build script or install script</then>
      </case>
    </suite>
  </requirement>
</behaviors>
