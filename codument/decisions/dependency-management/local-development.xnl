<decision #project.dependency_management.untracked_bun_lock apiVersion="codument.tech/v1alpha1" { status = "accepted" priority = "P0" blocks = [] durable_candidate = true } (
  <question ?>Eidolon 是否将 bun.lock 纳入版本管理，并以发布版本限制本地开发依赖？</?>
  <answer { } (
    <raw-answer ?>bun.lock 坚决不允许纳入版本管理。因为本项目开发过程常常需要引用本地其他文件夹的未发布版本的包。</?>
    <decision-text ?>bun.lock 保持不受 Git 跟踪，保留相应忽略规则；本地其他工作区未发布包引用是受支持的开发形态，不得把 registry-only 或受版本管理 lock 作为开发、Mission 实现的前置。</?>
    <rationale ?>开发需要跨项目联合迭代。具体一次构建/测试可保留未入版本库的本地 lock 与依赖内容身份回执；不能由复现需求反推出必须提交 lock 或禁止本地依赖。</?>
    <evidence ?>2026-09-05 用户明确纠正此前分析建议；两份 pending Mission 的 proposal/design 采用本约束。</?>
  )>
  <confidence ?>high</?>
  <reversibility ?>high；只有用户明确修订策略后才可改变，当前不执行任何锁文件跟踪变化。</?>
)>
