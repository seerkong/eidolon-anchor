# Modeling and Engineering Commands

Use the registry commands after changing durable structure or engineering knowledge.

```bash
codument modeling validate
codument modeling lint
codument engineering validate
codument engineering lint
```

`validate` checks the registry structure and references. `lint` applies additional content and consistency checks. A project without a modeling registry is allowed until a track has structural knowledge to record.
