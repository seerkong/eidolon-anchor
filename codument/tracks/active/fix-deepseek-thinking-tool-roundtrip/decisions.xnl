<decisions #fix-deepseek-thinking-tool-roundtrip {
  track = "fix-deepseek-thinking-tool-roundtrip"
}
(
  <decision #fix-deepseek-thinking-tool-roundtrip.wire-display-separation {
    status = "accepted"
    priority = "P0"
    durable_candidate = true
  }
  (
    <question ?>DeepSeek reasoning 的协议保真和 TUI 重复展示应如何协调？</?>
    <answer { }
    (
      <raw-answer ?>用户要求先使用 codument-plan-track，再使用 codument-impl-track 修复已确认的 DeepSeek interleaved thinking 工具调用问题。</?>
      <decision-text ?>始终保留 provider wire-level reasoning_content；TUI 是否显示镜像 thinking 使用独立策略，禁止通过删除协议字段实现展示去重。</?>
      <rationale ?>DeepSeek tools 协议要求后续请求完整回传 reasoning_content，而展示去重只是本地 presentation concern。</?>
      <evidence ?>故障 session turn 139 缺 reasoning_content 后，turn 140 返回明确的 HTTP 400；stream adapter 当前会删除空值和镜像值。</?>
    )>
  )>
  <decision #fix-deepseek-thinking-tool-roundtrip.legacy-recovery {
    status = "accepted"
    priority = "P0"
    durable_candidate = true
  }
  (
    <question ?>升级前已经缺失 reasoning_content 的历史 session 如何恢复？</?>
    <answer { }
    (
      <raw-answer ?>按无需修改历史 session 文件且可以继续现有会话的目标处理。</?>
      <decision-text ?>DeepSeek 请求归一化对缺字段的 assistant tool-call 历史消息补 reasoning_content 空字符串；不修改 XNL 持久化文件。</?>
      <rationale ?>原始 SSE 未持久化，真实 reasoning 无法重建；请求边界 fallback 是最小且可逆的兼容方案。</?>
      <evidence ?>故障 history 和 effect evidence 均已缺字段，provider 错误要求字段必须传回。</?>
    )>
  )>
  <decision #fix-deepseek-thinking-tool-roundtrip.execution-mode {
    status = "accepted"
    priority = "P1"
  }
  (
    <question ?>本 track 如何在 plan 后连续进入实现？</?>
    <answer { }
    (
      <raw-answer ?>用户明确要求先 plan-track，然后 impl-track。</?>
      <decision-text ?>QuestionSeverity 使用 auto，CommitMode 使用 manual，不配置额外 HumanConfirm、GapLoop 或 AttractorCheck。</?>
      <rationale ?>用户已预授权连续实现；自动决策使用 Codument 的保守默认且不产生隐式 commit。</?>
      <evidence ?>当前用户请求明确包含两个顺序动作。</?>
    )>
  )>
)>
