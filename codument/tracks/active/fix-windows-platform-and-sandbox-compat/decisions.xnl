<DecisionForest version="1" id="fix-windows-platform-and-sandbox-compat">
  <Decision id="windows-sandbox-level-model" owner="track">
    <Question>Windows sandbox 缺失时如何处理？</Question>
    <Options>
      <Option letter="A" name="fail-closed">runner 缺失时返回显式 error，不降级</Option>
      <Option letter="B" name="fail-open-unsandboxed">runner 缺失时走普通执行 + 权限检查</Option>
      <Option letter="C" name="tiered">三级：elevated/restricted-token/disabled</Option>
    </Options>
    <Conclusion>B+C 结合：三级模式，disabled 时走普通执行 + 权限检查，runner 可用时走 windows-elevated。对齐 Codex exec.rs:516。</Conclusion>
    <Rationale>fail-closed 让 Windows 用户完全无法使用 bash；分级降级 + 权限检查保留文件安全，且与 Codex 的 Disabled 模式一致。</Rationale>
    <Status>decided</Status>
  </Decision>
  <Decision id="platform-injection-point" owner="track">
    <Question>平台信息注入到哪里？</Question>
    <Options>
      <Option letter="A" name="system-prompt-only">只注入 buildSystemMessages</Option>
      <Option letter="B" name="metadata-only">只注入 normalizeTerminalRuntimeMetadata</Option>
      <Option letter="C" name="both">两者都注入</Option>
    </Options>
    <Conclusion>C。systemPrompt 让 agent 感知并选对命令；metadata 让 sandbox/permission 层有默认值（tui_a1 不传 metadata 也有合理默认）。</Conclusion>
    <Rationale>agent 感知靠 systemPrompt，runtime 行为靠 metadata，两者互补。</Rationale>
    <Status>decided</Status>
  </Decision>
  <Decision id="full-auto-default" owner="track">
    <Question>tui_a1 不传 metadata 时 exec_protocol.mode 默认值？</Question>
    <Options>
      <Option letter="A" name="full-auto-all-platforms">默认全自动（所有平台）</Option>
      <Option letter="B" name="full-auto-windows-only">仅 Windows 默认 full-auto，其他平台保持 interactive</Option>
      <Option letter="C" name="interactive">保持 interactive，需审批</Option>
    </Options>
    <Conclusion>B。Windows 无 headless 默认 full-auto + workspace-write 最可用（runner 缺失时可执行 bash）；其他平台保持 interactive，不改变既有审批行为（符合 Non-Goal "不改变非 Windows 平台的 sandbox 行为"）。文件权限检查仍保留。</Conclusion>
    <Rationale>当前 interactive 默认导致 Windows 上所有 bash 被 deny；full-auto 仅限 Windows 避免全局行为变更。LocalPermissionEvaluator 文件权限检查仍兜底。</Rationale>
    <Status>decided</Status>
  </Decision>
</DecisionForest>
