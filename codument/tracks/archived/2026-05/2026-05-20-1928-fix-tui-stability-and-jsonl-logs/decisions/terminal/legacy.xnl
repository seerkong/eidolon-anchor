<decision #track.fix-tui-stability-and-jsonl-logs.d1 apiVersion="codument.tech/v1alpha1" { status = "resolved" priority = "P0" durable_candidate = false } (
  <question ?>
非状态日志的归类边界：第一版迁移中，哪些日志调用要进入 JSONL append-only sink。
  </?>
  <recommendation ?>
C
  </?>
  <options { } [
    <option { key = "A" recommended = false } (
      <title ?>
仅按 logger namespace 白名单迁移
      </?>
      <description ?>
仅按 logger namespace 白名单迁移
      </?>
    )>
    <option { key = "B" recommended = false } (
      <title ?>
逐个调用点人工迁移
      </?>
      <description ?>
逐个调用点人工迁移
      </?>
    )>
    <option { key = "C" recommended = true } (
      <title ?>
先迁移 TUI 热路径里的 diagnostics，保留少量错误/边界日志的控制台镜像
      </?>
      <description ?>
先迁移 TUI 热路径里的 diagnostics，保留少量错误/边界日志的控制台镜像
      </?>
    )>
  ]>
  <answer { } (
    <raw-answer ?>
C
    </?>
    <decision-text ?>
C
    </?>
    <rationale ?>
先处理 runtime event receipt、sync dispatch、dialog input 这类高频 diagnostics，并保留错误、启动和提交等低频日志。
    </?>
    <evidence ?>
当前热路径里有 runtime receipt、sync listen、prompt input、bootstrap 等大量日志调用。

Legacy provenance: codument/tracks/archived/2026-05/2026-05-20-1928-fix-tui-stability-and-jsonl-logs/decisions.md

- 背景：当前热路径里有 runtime receipt、sync listen、prompt input、bootstrap 等大量日志调用。
- 需要决定：第一版迁移中，哪些日志调用要进入 JSONL append-only sink。
- 选项：
  - A) 仅按 logger namespace 白名单迁移
  - B) 逐个调用点人工迁移
  - C) 先迁移 TUI 热路径里的 diagnostics，保留少量错误/边界日志的控制台镜像
- 当前建议：C
- 用户答复：用户要求部分非状态类日志改为 JSONL append-only；未指定更细分类边界。
- 最终决策：C
- 决策理由：先处理 runtime event receipt、sync dispatch、dialog input 这类高频 diagnostics，并保留错误、启动和提交等低频日志。
- 状态：decided
    </?>
  )>
)>

<decision #track.fix-tui-stability-and-jsonl-logs.d2 apiVersion="codument.tech/v1alpha1" { status = "resolved" priority = "P1" durable_candidate = false } (
  <question ?>
JSONL 文件布局：文件是按进程、按天还是按 session 切分。
  </?>
  <recommendation ?>
A
  </?>
  <options { } [
    <option { key = "A" recommended = true } (
      <title ?>
按进程运行切分
      </?>
      <description ?>
按进程运行切分
      </?>
    )>
    <option { key = "B" recommended = false } (
      <title ?>
按天切分
      </?>
      <description ?>
按天切分
      </?>
    )>
    <option { key = "C" recommended = false } (
      <title ?>
按 session 切分
      </?>
      <description ?>
按 session 切分
      </?>
    )>
  ]>
  <answer { } (
    <raw-answer ?>
A
    </?>
    <decision-text ?>
A
    </?>
    <rationale ?>
运行实例级 JSONL 避免多个 TUI 进程争用同一个旧日志文件，同时仍可通过 `Log.path()` 定位。
    </?>
    <evidence ?>
append-only 日志需要易于检查，同时避免跨运行写放大。

Legacy provenance: codument/tracks/archived/2026-05/2026-05-20-1928-fix-tui-stability-and-jsonl-logs/decisions.md

- 背景：append-only 日志需要易于检查，同时避免跨运行写放大。
- 需要决定：文件是按进程、按天还是按 session 切分。
- 选项：
  - A) 按进程运行切分
  - B) 按天切分
  - C) 按 session 切分
- 当前建议：A
- 用户答复：未指定文件布局。
- 最终决策：A
- 决策理由：运行实例级 JSONL 避免多个 TUI 进程争用同一个旧日志文件，同时仍可通过 `Log.path()` 定位。
- 状态：decided
    </?>
  )>
)>

<decision #track.fix-tui-stability-and-jsonl-logs.d3 apiVersion="codument.tech/v1alpha1" { status = "resolved" priority = "P1" durable_candidate = false } (
  <question ?>
投影压降优先级：先做哪种边界收紧。
  </?>
  <recommendation ?>
C
  </?>
  <options { } [
    <option { key = "A" recommended = false } (
      <title ?>
先去重和节流日志，再收紧投影
      </?>
      <description ?>
先去重和节流日志，再收紧投影
      </?>
    )>
    <option { key = "B" recommended = false } (
      <title ?>
先收紧投影与缓存，再改日志
      </?>
      <description ?>
先收紧投影与缓存，再改日志
      </?>
    )>
    <option { key = "C" recommended = true } (
      <title ?>
两条线并行推进
      </?>
      <description ?>
两条线并行推进
      </?>
    )>
  ]>
  <answer { } (
    <raw-answer ?>
C
    </?>
    <decision-text ?>
C
    </?>
    <rationale ?>
日志同步写盘和重复 no-op 投影会叠加放大卡顿风险，需一起收敛。
    </?>
    <evidence ?>
runtime message/part 投影在长会话下会持续放大成本。

Legacy provenance: codument/tracks/archived/2026-05/2026-05-20-1928-fix-tui-stability-and-jsonl-logs/decisions.md

- 背景：runtime message/part 投影在长会话下会持续放大成本。
- 需要决定：先做哪种边界收紧。
- 选项：
  - A) 先去重和节流日志，再收紧投影
  - B) 先收紧投影与缓存，再改日志
  - C) 两条线并行推进
- 当前建议：C
- 用户答复：未指定优先级。
- 最终决策：C
- 决策理由：日志同步写盘和重复 no-op 投影会叠加放大卡顿风险，需一起收敛。
- 状态：decided
    </?>
  )>
)>
