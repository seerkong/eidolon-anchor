<decision #track.refactor-ai-runtime-control-primitives.d1 apiVersion="codument.tech/v1alpha1" { status = "accepted" priority = "P0" durable_candidate = false } (
  <question ?>
Vendor 原语包位置和名称：底层 actor control 原语包位置和名称。
  </?>
  <recommendation ?>
A
  </?>
  <options { } [
    <option { key = "A" recommended = true } (
      <title ?>
`vendor/depa-actor-control`
      </?>
      <description ?>
`vendor/depa-actor-control`
      </?>
    )>
    <option { key = "B" recommended = false } (
      <title ?>
`cell/packages/depa-actor-control`
      </?>
      <description ?>
`cell/packages/depa-actor-control`
      </?>
    )>
    <option { key = "C" recommended = false } (
      <title ?>
在既有 `depa-actor` 中新增子模块
      </?>
      <description ?>
在既有 `depa-actor` 中新增子模块
      </?>
    )>
  ]>
  <answer { } (
    <raw-answer ?>
A
    </?>
    <decision-text ?>
A
    </?>
    <rationale ?>
A 符合 vendor 原语优先，并避免把业务领域包与底层原语混在 cell package 下。
    </?>
    <evidence ?>
用户已在项目根目录创建 `vendor/`，并明确 `depa-actor-control` 应放到该目录下。

Legacy provenance: codument/tracks/archived/2026-06/2026-06-05-1620-refactor-ai-runtime-control-primitives/decisions.md

- 背景：用户已在项目根目录创建 `vendor/`，并明确 `depa-actor-control` 应放到该目录下。
- 需要决定：底层 actor control 原语包位置和名称。
- 选项：
  - A) `vendor/depa-actor-control`
  - B) `cell/packages/depa-actor-control`
  - C) 在既有 `depa-actor` 中新增子模块
- 当前建议：A
- 用户答复：用户明确要求放到项目根目录 `vendor/` 下。
- 最终决策：A
- 决策理由：A 符合 vendor 原语优先，并避免把业务领域包与底层原语混在 cell package 下。
- 状态：accepted
    </?>
  )>
)>

<decision #track.refactor-ai-runtime-control-primitives.d2 apiVersion="codument.tech/v1alpha1" { status = "accepted" priority = "P0" durable_candidate = false } (
  <question ?>
AI 领域控制面包名：AI 领域原语层包名。
  </?>
  <recommendation ?>
A
  </?>
  <options { } [
    <option { key = "A" recommended = true } (
      <title ?>
`cell/packages/ai-runtime-control-contract` 与 `cell/packages/ai-runtime-control-logic`
      </?>
      <description ?>
`cell/packages/ai-runtime-control-contract` 与 `cell/packages/ai-runtime-control-logic`
      </?>
    )>
    <option { key = "B" recommended = false } (
      <title ?>
`cell/packages/ai-control-contract` 与 `cell/packages/ai-control-logic`
      </?>
      <description ?>
`cell/packages/ai-control-contract` 与 `cell/packages/ai-control-logic`
      </?>
    )>
    <option { key = "C" recommended = false } (
      <title ?>
继续留在 `ai-organ-logic`
      </?>
      <description ?>
继续留在 `ai-organ-logic`
      </?>
    )>
  ]>
  <answer { } (
    <raw-answer ?>
A
    </?>
    <decision-text ?>
A
    </?>
    <rationale ?>
A 清楚表达 runtime 控制面语义，且符合 contract/logic 分离。
    </?>
    <evidence ?>
用户倾向于使用 `cell/packages/ai-runtime-control-contract` 与 `cell/packages/ai-runtime-control-logic`。

Legacy provenance: codument/tracks/archived/2026-06/2026-06-05-1620-refactor-ai-runtime-control-primitives/decisions.md

- 背景：用户倾向于使用 `cell/packages/ai-runtime-control-contract` 与 `cell/packages/ai-runtime-control-logic`。
- 需要决定：AI 领域原语层包名。
- 选项：
  - A) `cell/packages/ai-runtime-control-contract` 与 `cell/packages/ai-runtime-control-logic`
  - B) `cell/packages/ai-control-contract` 与 `cell/packages/ai-control-logic`
  - C) 继续留在 `ai-organ-logic`
- 当前建议：A
- 用户答复：用户明确表达倾向。
- 最终决策：A
- 决策理由：A 清楚表达 runtime 控制面语义，且符合 contract/logic 分离。
- 状态：accepted
    </?>
  )>
)>

<decision #track.refactor-ai-runtime-control-primitives.d3 apiVersion="codument.tech/v1alpha1" { status = "accepted" priority = "P0" durable_candidate = false } (
  <question ?>
第一批迁移范围：本 track 的第一批代码迁移范围。
  </?>
  <recommendation ?>
A
  </?>
  <options { } [
    <option { key = "A" recommended = true } (
      <title ?>
建立包与原语模型，并只迁移 snapshot safepoint 热路径
      </?>
      <description ?>
建立包与原语模型，并只迁移 snapshot safepoint 热路径
      </?>
    )>
    <option { key = "B" recommended = false } (
      <title ?>
同时迁移 heartbeat、questionnaire、actor surface 和 TUI actor 操作
      </?>
      <description ?>
同时迁移 heartbeat、questionnaire、actor surface 和 TUI actor 操作
      </?>
    )>
    <option { key = "C" recommended = false } (
      <title ?>
只写设计，不做代码迁移
      </?>
      <description ?>
只写设计，不做代码迁移
      </?>
    )>
  ]>
  <answer { } (
    <raw-answer ?>
A
    </?>
    <decision-text ?>
A
    </?>
    <rationale ?>
A 让新原语通过真实热路径验证，同时避免把架构整理扩大成多系统重写。
    </?>
    <evidence ?>
控制面问题覆盖 safepoint、heartbeat、questionnaire、actor surface 和 TUI actor 操作；一次性迁移全部风险较高。

Legacy provenance: codument/tracks/archived/2026-06/2026-06-05-1620-refactor-ai-runtime-control-primitives/decisions.md

- 背景：控制面问题覆盖 safepoint、heartbeat、questionnaire、actor surface 和 TUI actor 操作；一次性迁移全部风险较高。
- 需要决定：本 track 的第一批代码迁移范围。
- 选项：
  - A) 建立包与原语模型，并只迁移 snapshot safepoint 热路径
  - B) 同时迁移 heartbeat、questionnaire、actor surface 和 TUI actor 操作
  - C) 只写设计，不做代码迁移
- 当前建议：A
- 用户答复：
- 最终决策：A
- 决策理由：A 让新原语通过真实热路径验证，同时避免把架构整理扩大成多系统重写。
- 状态：accepted
    </?>
  )>
)>

<decision #track.refactor-ai-runtime-control-primitives.d4 apiVersion="codument.tech/v1alpha1" { status = "accepted" priority = "P1" durable_candidate = false } (
  <question ?>
兼容导出策略：迁移后是否保留兼容导出。
  </?>
  <recommendation ?>
B
  </?>
  <options { } [
    <option { key = "A" recommended = false } (
      <title ?>
保留薄 re-export/adapter，后续 track 再删除
      </?>
      <description ?>
保留薄 re-export/adapter，后续 track 再删除
      </?>
    )>
    <option { key = "B" recommended = true } (
      <title ?>
本 track 一次性更新所有调用方并删除旧路径
      </?>
      <description ?>
本 track 一次性更新所有调用方并删除旧路径
      </?>
    )>
    <option { key = "C" recommended = false } (
      <title ?>
只保留旧路径，新包不直接对外导出 safepoint API
      </?>
      <description ?>
只保留旧路径，新包不直接对外导出 safepoint API
      </?>
    )>
  ]>
  <answer { } (
    <raw-answer ?>
B
    </?>
    <decision-text ?>
B
    </?>
    <rationale ?>
B 避免形成第二个 API surface，符合 proposal、design、plan 与 spec delta 对 package name import 和单一事实来源的约束。
    </?>
    <evidence ?>
当前测试和调用方可能通过 `ai-organ-logic` 或 `RuntimeSnapshots` 深路径导入 safepoint API。

Legacy provenance: codument/tracks/archived/2026-06/2026-06-05-1620-refactor-ai-runtime-control-primitives/decisions.md

- 背景：当前测试和调用方可能通过 `ai-organ-logic` 或 `RuntimeSnapshots` 深路径导入 safepoint API。
- 需要决定：迁移后是否保留兼容导出。
- 选项：
  - A) 保留薄 re-export/adapter，后续 track 再删除
  - B) 本 track 一次性更新所有调用方并删除旧路径
  - C) 只保留旧路径，新包不直接对外导出 safepoint API
- 当前建议：B
- 用户答复：本 track 不保留旧 safepoint shim、兼容 re-export 或 adapter。
- 最终决策：B
- 决策理由：B 避免形成第二个 API surface，符合 proposal、design、plan 与 spec delta 对 package name import 和单一事实来源的约束。
- 状态：accepted
    </?>
  )>
)>
