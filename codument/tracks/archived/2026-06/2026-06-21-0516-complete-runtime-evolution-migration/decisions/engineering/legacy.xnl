<decision #track.complete-runtime-evolution-migration.d1 apiVersion="codument.tech/v1alpha1" { status = "accepted" priority = "P1" durable_candidate = false } (
  <question ?>
真实事故 conformance resource 形态
  </?>
  <options { } [
    <option { key = "A" recommended = true } (
      <title ?>
忠实最小化/脱敏 incident fixture（不含原始真实 session 数据）
      </?>
      <description ?>
忠实最小化/脱敏 incident fixture（不含原始真实 session 数据）
      </?>
    )>
    <option { key = "B" recommended = false } (
      <title ?>
提交脱敏后的真实 old-format session 文件
      </?>
      <description ?>
提交脱敏后的真实 old-format session 文件
      </?>
    )>
  ]>
  <answer { } (
    <raw-answer ?>
legacy source records a resolved decision without a separate raw answer
    </?>
    <decision-text ?>
真实事故 conformance resource 形态
    </?>
    <rationale ?>
legacy source did not record this field
    </?>
    <evidence ?>
deliverable 要「真实事故 session conformance resource」。真实 old-format session 在磁盘存在但 `.eidolon` gitignored 且含潜在敏感内容。

Legacy provenance: codument/tracks/archived/2026-06/2026-06-21-0516-complete-runtime-evolution-migration/decisions.md

- 背景：deliverable 要「真实事故 session conformance resource」。真实 old-format session 在磁盘存在但 `.eidolon` gitignored 且含潜在敏感内容。
- 选项：
  - A) 忠实最小化/脱敏 incident fixture（不含原始真实 session 数据）
  - B) 提交脱敏后的真实 old-format session 文件
- 编排器默认：**A**（隐私安全）
- 理由：满足 deliverable「重现真实事故形态」之意图，同时避免把潜在敏感的真实用户 session 内容提交进仓库；与 backplane track 已接受最小化 005 fixture 的先例一致。可重现事故根因（重复读/pending effect/history 滞后）即足够过 gate。
- 状态：accepted（默认，非阻塞；用户可推翻改 B）
    </?>
  )>
)>

<decision #track.complete-runtime-evolution-migration.d2 apiVersion="codument.tech/v1alpha1" { status = "accepted" priority = "P1" durable_candidate = false } (
  <question ?>
CLI/TUI/headless 验收 harness 深度
  </?>
  <options { } [
    <option { key = "A" recommended = true } (
      <title ?>
聚焦 conformance run（升级真实形态 old session → 跨 CLI/TUI/headless replay 一个 turn，断言事故根因不复现 + 跨表现等价）
      </?>
      <description ?>
聚焦 conformance run（升级真实形态 old session → 跨 CLI/TUI/headless replay 一个 turn，断言事故根因不复现 + 跨表现等价）
      </?>
    )>
    <option { key = "B" recommended = false } (
      <title ?>
全量多轮长运行 soak
      </?>
      <description ?>
全量多轮长运行 soak
      </?>
    )>
  ]>
  <answer { } (
    <raw-answer ?>
legacy source records a resolved decision without a separate raw answer
    </?>
    <decision-text ?>
CLI/TUI/headless 验收 harness 深度
    </?>
    <rationale ?>
legacy source did not record this field
    </?>
    <evidence ?>
deliverable 要 CLI/TUI/headless 长运行验收 harness。

Legacy provenance: codument/tracks/archived/2026-06/2026-06-21-0516-complete-runtime-evolution-migration/decisions.md

- 背景：deliverable 要 CLI/TUI/headless 长运行验收 harness。
- 选项：
  - A) 聚焦 conformance run（升级真实形态 old session → 跨 CLI/TUI/headless replay 一个 turn，断言事故根因不复现 + 跨表现等价）
  - B) 全量多轮长运行 soak
- 编排器默认：**A**（audit 推荐）
- 理由：聚焦 conformance 足够验证 gate `G-migration-incident`（事故根因修复 + 跨表现一致），更省、更稳（避免长 soak 的 flaky）；与本 mission 一贯的「验证优先、最小可执行覆盖」一致。
- 状态：accepted（默认，非阻塞；用户可推翻改 B）
    </?>
  )>
)>

<decision #track.complete-runtime-evolution-migration.d3 apiVersion="codument.tech/v1alpha1" { status = "accepted" priority = "P1" durable_candidate = false } (
  <question ?>
提交/校验/方向审查模式
  </?>
  <options { } [
    <option { key = "A" recommended = true } (
      <title ?>
沿用前 4 track
      </?>
      <description ?>
沿用前 4 track
      </?>
    )>
    <option { key = "B" recommended = false } (
      <title ?>
自定义
      </?>
      <description ?>
自定义
      </?>
    )>
  ]>
  <answer { } (
    <raw-answer ?>
legacy source records a resolved decision without a separate raw answer
    </?>
    <decision-text ?>
提交/校验/方向审查模式
    </?>
    <rationale ?>
legacy source did not record this field
    </?>
    <evidence ?>
Legacy provenance: codument/tracks/archived/2026-06/2026-06-21-0516-complete-runtime-evolution-migration/decisions.md

- 选项：A) 沿用前 4 track；B) 自定义
- 编排器默认：**A** — CommitMode=manual；终态 phase 挂 `<cdt:GapLoop max-rounds="5" on-exhausted="block"/>`；每个第一层 phase 挂 `<cdt:AttractorCheck use="coding"/>`。
- 理由：与 lifecycle/backplane/surfaces/multi-agent 四条已归档 track 一致。
- 状态：accepted
    </?>
  )>
)>
