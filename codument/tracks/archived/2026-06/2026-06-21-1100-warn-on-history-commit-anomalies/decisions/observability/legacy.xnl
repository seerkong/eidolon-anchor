<decision #track.warn-on-history-commit-anomalies.d1 apiVersion="codument.tech/v1alpha1" { status = "resolved" priority = "P0" durable_candidate = false } (
  <question ?>
告警外溢通道：用什么通道把异常告警从 MessageHistoryGraph 送到可观测处。
  </?>
  <recommendation ?>
A（最贴合既有事件驱动架构、reducer 纯、宿主集中记录、易 grep）
  </?>
  <options { } [
    <option { key = "A" recommended = true } (
      <title ?>
新增一类结构化 anomaly/warning 事件，经既有 listener 通道外溢，宿主侧 `console.warn` + 可选诊断记录（与 `emit`/`emitCommitted` 同构，reducer 保持纯）
      </?>
      <description ?>
新增一类结构化 anomaly/warning 事件，经既有 listener 通道外溢，宿主侧 `console.warn` + 可选诊断记录（与 `emit`/`emitCommitted` 同构，reducer 保持纯）
      </?>
    )>
    <option { key = "B" recommended = false } (
      <title ?>
复用既有 diagnostics/observability sink 直接记录
      </?>
      <description ?>
复用既有 diagnostics/observability sink 直接记录
      </?>
    )>
    <option { key = "C" recommended = false } (
      <title ?>
其他
      </?>
      <description ?>
其他
      </?>
    )>
  ]>
  <answer { } (
    <raw-answer ?>
A（安全默认，非阻塞）
    </?>
    <decision-text ?>
A（安全默认，非阻塞）
    </?>
    <rationale ?>
保持唯一水源与纯 reducer 不被破坏；告警走与提交同构的事件通道，宿主统一落日志/诊断。
    </?>
    <evidence ?>
reducer 是纯函数，不能在其中 `console.warn`。告警必须经事件/可观测通道外溢由宿主记录。

Legacy provenance: codument/tracks/archived/2026-06/2026-06-21-1100-warn-on-history-commit-anomalies/decisions.md

- 背景：reducer 是纯函数，不能在其中 `console.warn`。告警必须经事件/可观测通道外溢由宿主记录。
- 需要决定：用什么通道把异常告警从 MessageHistoryGraph 送到可观测处。
- 选项：
  - A) 新增一类结构化 anomaly/warning 事件，经既有 listener 通道外溢，宿主侧 `console.warn` + 可选诊断记录（与 `emit`/`emitCommitted` 同构，reducer 保持纯）
  - B) 复用既有 diagnostics/observability sink 直接记录
  - C) 其他
- 当前建议：A（最贴合既有事件驱动架构、reducer 纯、宿主集中记录、易 grep）
- 最终决策：A（安全默认，非阻塞）
- 决策理由：保持唯一水源与纯 reducer 不被破坏；告警走与提交同构的事件通道，宿主统一落日志/诊断。
- 状态：resolved（安全默认，实现期可微调宿主记录细节）
    </?>
  )>
)>

<decision #track.warn-on-history-commit-anomalies.d2 apiVersion="codument.tech/v1alpha1" { status = "resolved" priority = "P0" durable_candidate = false } (
  <question ?>
告警是 warning 非异常，且不改提交内容
  </?>
  <answer { } (
    <raw-answer ?>
观测-only；warn + continue；保留唯一水源、不加兜底写者。
    </?>
    <decision-text ?>
观测-only；warn + continue；保留唯一水源、不加兜底写者。
    </?>
    <rationale ?>
单写者/唯一水源是核心设计，加兜底写者会掩盖故障、积技术债；正确加固是"失败即出声"。
    </?>
    <evidence ?>
用户明确"产生 warning、快速定位",不是抛错。

Legacy provenance: codument/tracks/archived/2026-06/2026-06-21-1100-warn-on-history-commit-anomalies/decisions.md

- 背景：用户明确"产生 warning、快速定位",不是抛错。
- 决定：异常交互**只新增告警**，**不改变实际被提交的消息**（孤儿 tool 消息仍按现状提交、空壳仍按现状提交），**不抛错**、不中断 turn。
- 最终决策：观测-only；warn + continue；保留唯一水源、不加兜底写者。
- 决策理由：单写者/唯一水源是核心设计，加兜底写者会掩盖故障、积技术债；正确加固是"失败即出声"。
- 状态：resolved
    </?>
  )>
)>

<decision #track.warn-on-history-commit-anomalies.d3 apiVersion="codument.tech/v1alpha1" { status = "resolved" priority = "P1" durable_candidate = false } (
  <question ?>
孤儿判定的配对来源
  </?>
  <answer { } (
    <raw-answer ?>
reducer 内已见-id 集合判定。
    </?>
    <decision-text ?>
reducer 内已见-id 集合判定。
    </?>
    <rationale ?>
纯 reducer 可携带该集合作为投影状态，无需外部查询。
    </?>
    <evidence ?>
判断一个 tool result 是否"孤儿",需要已知该 turn/generation 见过哪些 assistant 工具调用 id。

Legacy provenance: codument/tracks/archived/2026-06/2026-06-21-1100-warn-on-history-commit-anomalies/decisions.md

- 背景：判断一个 tool result 是否"孤儿",需要已知该 turn/generation 见过哪些 assistant 工具调用 id。
- 决定：reducer 内维护"已见 tool_call_id 集合"（来自 `semantic_tool_call_start/_planned` + pending/已提交 assistant 的 toolCalls）；result 的 id 不在集合内 → 孤儿。
- 最终决策：reducer 内已见-id 集合判定。
- 决策理由：纯 reducer 可携带该集合作为投影状态，无需外部查询。
- 状态：resolved
    </?>
  )>
)>
