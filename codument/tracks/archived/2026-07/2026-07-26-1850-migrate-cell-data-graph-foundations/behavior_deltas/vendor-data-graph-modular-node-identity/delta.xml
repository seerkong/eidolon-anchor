<?xml version="1.0" encoding="UTF-8"?>
<behavior-patch capability="vendor-data-graph-modular-node-identity" version="1">
  <upsert selector="behavior://vendor-data-graph-modular-node-identity/requirements/cell-stage-graph-unified-consumer-contract">
    <requirement id="cell-stage-graph-unified-consumer-contract">
      <statement>Cell stage graph foundations SHALL use the unified DataGraph consumer contract while preserving lexical-to-syntactic-to-semantic event ordering, callback delivery, module identity, and focused runtime behavior.</statement>
      <suite id="stage-consumer-migration" name="Stage consumer migration">
        <case id="consumer-callbacks-read-current-state-through-unified-contract">
          <given>ReferenceAlignedStageDataGraph registers consumers for lexical, syntactic, and semantic state transitions</given>
          <when>the graph runs on depa-data-graph 1.0.1</when>
          <then>consumer callbacks SHALL NOT rely on the removed callback ctx.get shape</then>
          <then>they SHALL read the intended current graph state through the supported unified graph contract</then>
        </case>
        <case id="stage-pipeline-focused-behavior-remains-stable">
          <given>existing live/replay stage and semantic stream fixtures</given>
          <when>the unified consumer callbacks process lexical input</when>
          <then>they SHALL retain ordered stage outputs and callback-visible events</then>
        </case>
      </suite>
    </requirement>
  </upsert>
</behavior-patch>
