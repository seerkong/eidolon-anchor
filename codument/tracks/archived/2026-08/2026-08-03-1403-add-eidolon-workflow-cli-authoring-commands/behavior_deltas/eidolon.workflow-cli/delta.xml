<behavior-patch capability="eidolon.workflow-cli" version="1">
  <upsert selector="behavior://eidolon.workflow-cli/requirements/component-backed-authoring">
    <requirement id="component-backed-authoring">
      <statement>The Eidolon CLI SHALL expose workflow authoring commands that reuse the Eidolon workflow component/service instead of implementing independent workflow core logic.</statement>
      <suite id="workflow-init" name="Workflow init">
        <case id="init-writes-controlled-draft">
          <given>a user runs `eidolon workflow init ai-data demo`</given>
          <when>the command creates files</when>
          <then>the CLI SHALL obtain the bundle draft from the workflow component and write the returned files under the selected `.eidolon` target root</then>
        </case>
      </suite>
    </requirement>
  </upsert>

  <upsert selector="behavior://eidolon.workflow-cli/requirements/workflow-roots-metadata">
    <requirement id="workflow-roots-metadata">
      <statement>Headless Eidolon runtime metadata SHALL include AI workflow roots so native workflow tools can detect workspace/global workflow roots during CLI conversations.</statement>
      <suite id="exec-runtime-metadata" name="Exec runtime metadata">
        <case id="inject-aiworkflow-roots">
          <given>headless exec runtime metadata is built</given>
          <when>WorkflowInspectCapability runs in that runtime</when>
          <then>the tool SHALL report workflowRootsInjected=true</then>
        </case>
      </suite>
    </requirement>
  </upsert>
</behavior-patch>
