  <decision #refactor-chat-completions-effect-bundles.composition apiVersion="codument.tech/v1alpha1" {
    status = "accepted"
    priority = "P0"
    durable_candidate = true
  }
  (
    <question ?>OpenAI Chat 与 DeepSeek 应复制两个 adapter，还是共享核心并提供两个 effect bundle？</?>
    <answer { }
    (
      <raw-answer ?>用户确认共同部分多时，应将 DeepSeek 和 OpenAI 官方版本做成一个 effect bundle contract 的两个实现。</?>
      <decision-text ?>建立一个 Chat Completions 公共核心，以及 OpenAI official Chat 和 DeepSeek official Chat 两个 effect bundle 实现。</?>
      <rationale ?>两者的 transport、SSE、content/tool-call 和 ingress 机制高度相同，差异集中在 provider protocol projection。</?>
      <evidence ?>当前两个 provider driver 均实例化同一 fetch adapter，stream adapter 中只有 reasoning 和少量 endpoint/request 分支体现差异。</?>
    )>
  <confidence ?>0.95</?>
  <reversibility ?>moderate</?>
  )>
  <decision #refactor-chat-completions-effect-bundles.pure-core apiVersion="codument.tech/v1alpha1" {
    status = "accepted"
    priority = "P0"
    durable_candidate = true
  }
  (
    <question ?>共享流状态机如何与副作用隔离？</?>
    <answer { }
    (
      <raw-answer ?>用户要求将内部逻辑改造为纯函数，供两个 adapter 组合使用。</?>
      <decision-text ?>以纯 reducer 处理 chunk、tool-call、reasoning、think-tag 与 final message；fetch、timeout、abort 和 OutputStream 发送留在 effect shell。</?>
      <rationale ?>该边界符合项目 DOP、显式 effect 与可验证性 attractor。</?>
      <evidence ?>现有 stream adapter 将数据归约和 OutputStream 写入混在同一可变 class 中。</?>
    )>
  <confidence ?>0.95</?>
  <reversibility ?>moderate</?>
  )>
  <decision #refactor-chat-completions-effect-bundles.explicit-selection apiVersion="codument.tech/v1alpha1" {
    status = "accepted"
    priority = "P0"
    durable_candidate = true
  }
  (
    <question ?>effect bundle 应如何选择？</?>
    <answer { }
    (
      <raw-answer ?>沿用用户确认的两个官方实现方案。</?>
      <decision-text ?>由 ProviderDriverRegistry 已选中的 driver 显式注入 bundle，并把本次请求实际使用的 adapter/driver binding 传到 stream callback 与 ingress；移除模型名、Base URL 和流侧 adapter-type 的二次选择。</?>
      <rationale ?>adapter 配置是协议选择真源；若请求侧使用 driver、流侧再按字符串选择，会形成两个可能漂移的真源并误分类第三方 endpoint 或 provider switch。</?>
      <evidence ?>ProviderRuntimeLlmAdapter 已持有 selected driver，但 AiAgentExecutor 只把 raw stream 交给 callback；TerminalRuntime 随后从 llmClient.type 重建 streamAdapterType。</?>
    )>
  <confidence ?>0.95</?>
  <reversibility ?>moderate</?>
  )>
  <decision #refactor-chat-completions-effect-bundles.domain-ownership apiVersion="codument.tech/v1alpha1" {
    status = "accepted"
    priority = "P0"
    durable_candidate = true
  }
  (
    <question ?>Chat Completions stream core 与兼容 facade 应归属 symbiont 平台层还是 AI provider 领域层？</?>
    <answer { }
    (
      <raw-answer ?>After-design coding AttractorCheck 要求对照双层微内核、contract/logic 分离与禁止反向依赖审查。</?>
      <decision-text ?>将 Chat Completions、reasoning 与 tool-call reducer/facade 迁入 ai-organ contract/logic；symbiont 只保留 provider-neutral stream/runtime 原语。</?>
      <rationale ?>provider、tool calling protocol 和 AI semantic projection 属于 AI domain kernel；symbiont-logic 无 AI contract 依赖，继续放置会迫使反向依赖或双份 policy。</?>
      <evidence ?>现有 symbiont stream adapter 直接解释 reasoning_content、reasoning_details 与 tool_calls，package.json 仅依赖 symbiont-contract。</?>
    )>
  <confidence ?>0.95</?>
  <reversibility ?>moderate</?>
  )>
  <decision #refactor-chat-completions-effect-bundles.responses-scope apiVersion="codument.tech/v1alpha1" {
    status = "accepted"
    priority = "P1"
  }
  (
    <question ?>OpenAI 官方版本是否包含 Responses API？</?>
    <answer { }
    (
      <raw-answer ?>当前讨论针对共享 OpenAI-compatible Chat Completions 流适配器。</?>
      <decision-text ?>本 track 的 OpenAI official 仅指 Chat Completions；Responses API 保持独立 driver 和 transport。</?>
      <rationale ?>Responses 的事件、continuation id 与 tool protocol 与 Chat Completions 差异显著，缺少稳定共性。</?>
      <evidence ?>代码库已维护独立 OpenAIResponsesDriver 与 OpenAIResponsesNodejsFetchAdapter。</?>
    )>
  <confidence ?>0.95</?>
  <reversibility ?>moderate</?>
  )>
  <decision #refactor-chat-completions-effect-bundles.execution apiVersion="codument.tech/v1alpha1" {
    status = "accepted"
    priority = "P1"
  }
  (
    <question ?>规划完成后如何执行与校验？</?>
    <answer { }
    (
      <raw-answer ?>用户明确要求先运行 codument-plan-track，然后运行 codument-impl-track。</?>
      <decision-text ?>QuestionSeverity 使用 auto，CommitMode 使用 manual；最后一个 phase 执行 coding AttractorCheck，不配置 HumanConfirm 或 GapLoop。</?>
      <rationale ?>用户已经授权连续实现；架构变更需要方向审查但不应引入隐式 commit 或额外人工暂停。</?>
      <evidence ?>当前请求明确给出顺序动作，项目 modeling 与 engineering registry 均显式 disabled。</?>
    )>
  <confidence ?>0.95</?>
  <reversibility ?>moderate</?>
  )>
