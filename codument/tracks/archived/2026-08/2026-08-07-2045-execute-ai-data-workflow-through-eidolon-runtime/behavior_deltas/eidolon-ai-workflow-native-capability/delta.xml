<behavior-patch capability="eidolon-ai-workflow-native-capability" version="1">
  <upsert selector="behavior://eidolon-ai-workflow-native-capability/requirements/ai-data-runtime">
    <requirement id="ai-data-runtime">
      <statement>The system SHALL execute AIDataWorkflow as a persisted, per-run mutable DAG over canonical EagerDataFlow and AIDataWorkflow RunGraph semantics.</statement>
      <suite id="data-runtime" name="AIDataWorkflow runtime">
        <case id="node-state"><given>A canonical AIDataWorkflow resource exists</given><when>WorkflowRun starts it</when><then>The system SHALL persist stable nodes with inputs, outputs, config, status, results and generation</then></case>
        <case id="graph-patch"><given>A completed or waiting run graph</given><when>A structurally valid GraphPatch is applied</when><then>The system SHALL create a new generation, record the patch and transitively invalidate affected nodes</then></case>
        <case id="semantic-reuse"><given>An invalidated machine node has unchanged semantic input, config, material revisions and effect policy in the same run</given><when>The new generation advances</when><then>The system SHALL reuse the prior result without re-executing the node and SHALL retain reuse provenance</then></case>
        <case id="never-policy"><given>A manual node or explicit node policy resolves to never</given><when>A new generation advances</when><then>The system SHALL execute or wait again and SHALL record the policy source</then></case>
        <case id="recovery"><given>A persisted graph contains completed and waiting facts</given><when>A fresh Eidolon VM queries or resumes it</when><then>The system SHALL restore the RunGraph and continue from stable node identities</then></case>
      </suite>
    </requirement>
  </upsert>
</behavior-patch>
