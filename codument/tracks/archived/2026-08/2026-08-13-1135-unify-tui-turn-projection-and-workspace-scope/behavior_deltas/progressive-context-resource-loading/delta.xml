<behavior-patch capability="progressive-context-resource-loading" version="1">
  <upsert selector="behavior://progressive-context-resource-loading/requirements/context-resource-surface-projection">
    <requirement id="context-resource-surface-projection">
      <statement>Runtime/tool projection SHALL 向 surface 提供 typed context resource metadata；TUI SHALL 以折叠 CONTEXT/read 卡片显示 resource id、revision、range、size 与 status，SHALL NOT 把 `&lt;context-resource&gt;` raw wrapper 显示为 USER，也 SHALL NOT 在 UI 以正则或模糊文本匹配恢复语义。</statement>
      <suite id="context-card" name="上下文资源卡片">
        <case id="loaded-resource-collapsed">
          <given>read tool 交付一个 loaded context resource</given>
          <when>TUI 投影其 tool result</when>
          <then>surface SHALL 显示 workspace-relative resource、行范围、大小与 revision 摘要</then>
          <then>正文 SHALL 可按需展开而非默认占满 USER 卡片</then>
        </case>
        <case id="already-visible-is-weak">
          <given>context resource status 为 already-visible</given>
          <when>TUI 投影结果</when>
          <then>surface SHALL 弱化或隐藏重复正文提示，同时保留可检查 metadata</then>
        </case>
        <case id="cold-hydration-preserves-context-card">
          <given>read tool result 已通过既有 conversation persistence 持久化并发生进程重启</given>
          <when>TUI 通过 ConversationProjectionReadPort 冷启动 hydrate history</when>
          <then>surface SHALL 从 committed tool result 的 typed metadata 恢复 CONTEXT/read 卡片</then>
          <then>surface MUST NOT 解析 raw context-resource XML 或建立第二个 persistence authority</then>
        </case>
      </suite>
    </requirement>
  </upsert>
</behavior-patch>
